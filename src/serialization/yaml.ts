import YAML from 'yaml';import {fail,ok,ERROR_CODES} from '../domain/errors.js';import type {ValidationResult} from '../domain/types.js';
function sortDeep(v:unknown):unknown{if(Array.isArray(v))return v.map(sortDeep);if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v as Record<string,unknown>).sort(([a],[b])=>a.localeCompare(b)).map(([k,x])=>[k,sortDeep(x)]));return v;}
export function stableYaml(value:unknown):string{return YAML.stringify(sortDeep(value),{lineWidth:0,aliasDuplicateObjects:false}).replace(/\r\n/g,'\n').replace(/\n*$/,'\n');}
export function parseYamlDocument<T>(content:string,location:string):ValidationResult<T>{try{const doc=YAML.parseDocument(content,{customTags:[],maxAliasCount:20});if(doc.errors.length)return fail(...doc.errors.map(e=>({code:ERROR_CODES.INVALID_YAML,message:e.message,location})));return ok(doc.toJS() as T);}catch(e){return fail({code:ERROR_CODES.INVALID_YAML,message:e instanceof Error?e.message:String(e),location});}}
